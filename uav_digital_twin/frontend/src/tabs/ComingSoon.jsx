export function ComingSoon({ title }) {
  return (
    <section className="card placeholder">
      <h2>{title}</h2>
      <p>This view is next on the list. The live stream keeps running in the background.</p>
    </section>
  )
}
